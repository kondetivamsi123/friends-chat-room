<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Create a specific user for testing -->
    <record id="user_vamsi" model="res.users">
        <field name="name">Vamsi</field>
        <field name="login">vamsi</field>
        <field name="password">Vamsi</field>
        <field name="email">vamsi@example.com</field>
        <!-- Preset MFA for convenience -->
        <field name="mfa_enabled" eval="True"/>
        <field name="mfa_secret">123456</field> 
        <field name="groups_id" eval="[(4, ref('base.group_user'))]"/>
    </record>
</odoo>
